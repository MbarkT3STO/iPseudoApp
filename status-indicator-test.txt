# Status Indicator Test
# This file tests the console status indicator accuracy

# Test 1: Normal execution (should show "Running" then "Ready")
var name = "Test"
print "Hello " + name

# Test 2: Error case (should show "Running" then "Error")
var undefinedVar = nonExistentFunction()

# Test 3: Loop execution (should show "Running" during execution)
for i = 1 to 5
    print "Count: " + i
endfor

# Test 4: Function with error (should show "Error")
function testFunction()
    var result = 10 / 0  # This will cause an error
    return result
endfunction

testFunction()

print "Test completed"
