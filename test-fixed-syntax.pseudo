Algorithm TestFixedSyntax

/* Test the corrected syntax */

# Correct Global/Local syntax
Global Variable totalCount = 0
Global Var maxRetries = 3
Local Variable tempValue = 5
Local Var counter = 0

# Test Repeat-Until loop
Repeat
    Print "Counter:", counter
    counter = counter + 1
Until counter >= 3

# Test that variables are recognized
Print "Total count:", totalCount
Print "Max retries:", maxRetries
Print "Temp value:", tempValue

Endalgorithm
