{
  "name": "ipseudo-app",
  "version": "1.0",
  "description": "A modern Pseudocode IDE built with Electron",
  "main": "dist/main/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "tsc && npm run copy-files && electron .",
    "build": "tsc && npm run copy-files",
    "watch": "tsc -w",
    "copy-files": "mkdir -p dist/renderer && cp -r src/renderer/* dist/renderer/ && mkdir -p dist/renderer/monaco && cp -r node_modules/monaco-editor/min/* dist/renderer/monaco/"
  },
  "author": "",
  "license": "SEE LICENSE IN LICENSE",
  "devDependencies": {
    "@types/electron": "^1.6.10",
    "@types/node": "^20.8.0",
    "electron": "^38.1.2",
    "electron-builder": "^24.6.4",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "monaco-editor": "^0.43.0"
  }
}
